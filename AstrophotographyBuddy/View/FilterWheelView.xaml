<UserControl x:Class="AstrophotographyBuddy.View.FilterWheelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrophotographyBuddy.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding FW.Filters}" SelectedIndex="{Binding FW.Position}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Filter Name" Width="Auto" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Focus Offset" Width="Auto" DisplayMemberBinding="{Binding FocusOffset}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
