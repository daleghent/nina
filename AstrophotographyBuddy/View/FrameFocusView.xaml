<UserControl x:Class="AstrophotographyBuddy.View.FrameFocusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstrophotographyBuddy.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="90"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Gamma}" Visibility="{Binding ImgSource.IsNotCompleted, Converter={StaticResource VisibilityConverter}}" />
            <Image Grid.Column="0" Grid.Row="0" Source="{Binding ImgSource.Result}"/>
        <Button Grid.Column="0" Grid.Row="1" Width="50" Height="50"  IsEnabled="{Binding Cam.Connected}" Command="{Binding SnapCommand}">Preview</Button>
        <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
            <Slider Orientation="Vertical" Width="30" Value="{Binding Brightness}"  Maximum="255" Minimum="-255" SmallChange="1"></Slider>
            <Slider Orientation="Vertical" Width="30" Value="{Binding Contrast}" Maximum="100" Minimum="-100" SmallChange="1"></Slider>
            <Slider Orientation="Vertical" Width="30" Value="{Binding Gamma}" Maximum="5" Minimum="0.2" SmallChange="0.1"></Slider>
        </StackPanel>

        <Button Grid.Column="1" Grid.Row="1" Command="{Binding ApplyImageParamsCommand}">Apply</Button>
    </Grid>
</UserControl>
