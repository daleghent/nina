<Window x:Class="NINA.MyMessageBox.MyMessageBoxView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NINA.MyMessageBox"
        xmlns:util="clr-namespace:NINA.Utility"
        xmlns:ns="clr-namespace:NINA.Locale"
        util:DialogCloser.DialogResult="{Binding DialogResult}" 
        mc:Ignorable="d" MinHeight="155" MinWidth="250"
        Title="{Binding Title}"  ResizeMode="NoResize" SizeToContent="WidthAndHeight" Background="{StaticResource BackgroundBrush}">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"  />
        </Grid.RowDefinitions>    
        
        <TextBlock Text="{Binding Text}" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <UniformGrid Columns="2" Grid.Row="1">            
            <Button IsCancel="True" Click="Button_Cancel_Click" Visibility="{Binding CancelVisibility}">
                <TextBlock Foreground="{StaticResource ButtonForegroundBrush}" Text="{ns:Loc LblCancel}" />
            </Button>
            <Button IsDefault="True" Click="Button_OK_Click">
                <TextBlock Foreground="{StaticResource ButtonForegroundBrush}" Text="{ns:Loc LblOK}" />
            </Button>
        </UniformGrid>
    </Grid>
</Window>
