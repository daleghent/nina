<UserControl
    x:Class="NINA.View.Equipment.AlnitakSetupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NINA.View.Equipment"
    xmlns:locale="clr-namespace:NINA.Locale"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ports="clr-namespace:System.IO.Ports;assembly=System"
    xmlns:utility="clr-namespace:NINA.Utility"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <UniformGrid VerticalAlignment="Center">
            <TextBlock
                Margin="6,6,6,0"
                VerticalAlignment="Center"
                Text="Choose Serial Port"
                TextWrapping="NoWrap" />
            <ComboBox
                Name="SerialPorts"
                Margin="6,6,6,0"
                SelectedValue="{Binding PortName, Mode=TwoWay}" />
        </UniformGrid>
        <Button
            Margin="6"
            VerticalAlignment="Bottom"
            utility:ButtonHelper.DialogResult="True">
            <Button.Content>
                <TextBlock Foreground="{StaticResource ButtonForegroundBrush}" Text="{locale:Loc LblOK}" />
            </Button.Content>
        </Button>
    </Grid>
</UserControl>