<UserControl
    x:Class="NINA.View.AnchorableExposureCalculatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NINA.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ns="clr-namespace:NINA.Locale"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <StackPanel Margin="5" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="{ns:Loc LblMinExposureADU}" />
                        <TextBlock
                            Width="80"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding MinExposureADU, StringFormat=\{0:#0\}, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="{ns:Loc LblMaxExposureADU}" />
                        <TextBlock
                            Width="80"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding MaxExposureADU, StringFormat=\{0:#\}, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="{ns:Loc LblInOptimalExposure}" />
                        <TextBlock
                            Width="80"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding IsBetweenMinAndMax, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="{ns:Loc LblOptimalExposure}" />
                        <TextBlock
                            Width="80"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding RecommendedExposureTime, Mode=OneWay}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Width="80"
                            VerticalAlignment="Center"
                            Text="{ns:Loc LblMean}" />
                        <TextBlock
                            Width="80"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="{Binding Mean, StringFormat=\{0:#.00\}}" />
                    </StackPanel>
                </StackPanel>
            </Border>
            
            <Border
                Grid.Row="1"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1">
                <local:HistogramView MinHeight="80" />
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
