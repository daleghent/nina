<UserControl x:Class="NINA.View.CameraControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NINA.View"
             xmlns:ninactrl="clr-namespace:NINACustomControlLibrary;assembly=NINACustomControlLibrary"
             xmlns:ns="clr-namespace:NINA.Locale"
             xmlns:rules="clr-namespace:NINA.Utility.ValidationRules"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel x:Name="LayoutRoot" Orientation="{Binding MyOrientation}">
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>-->

        <StackPanel Orientation="{Binding MyOrientation}"  IsEnabled="{Binding MyCommand.Execution.IsNotCompleted, Converter={StaticResource InverseBooleanConverter}}">
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                <UniformGrid Columns="2" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center" Text="{ns:Loc LblExposureTime}"></TextBlock>
                    <TextBox Margin="5" MinWidth="50" >
                        <TextBox.Text>
                            <Binding Path="MyExposureDuration" Mode="TwoWay">
                                <Binding.ValidationRules>
                                    <rules:GreaterZeroRule />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                </UniformGrid>
            </Border>
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                <UniformGrid Columns="2" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center"  Text="{ns:Loc LblFilter}"></TextBlock>
                    <ComboBox Margin="5,0,0,0"  Height="25" ItemsSource="{Binding MyFilters}" DisplayMemberPath="Name" SelectedItem="{Binding MySelectedFilter}" SelectedValuePath="Name"></ComboBox>
                </UniformGrid>
            </Border>
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                <UniformGrid Columns="2" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center" Text="{ns:Loc LblBinning}"></TextBlock>
                    <ComboBox Margin="5,0,0,0" Height="25" ItemsSource="{Binding MyBinningModes}" DisplayMemberPath="Name" SelectedItem="{Binding MySelectedBinningMode}" SelectedValuePath="Name"></ComboBox>
                </UniformGrid>
            </Border>
        </StackPanel>

        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Visibility="{Binding MyCommand, Converter={StaticResource NullToVisibilityCollapsedConverter}}" Margin="5,0,0,0">

            <ToggleButton IsChecked="{Binding MyLoop}" Width="45" Height="45">
                <ToggleButton.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="True">
                        <TextBlock Text="{ns:Loc LblTooltipLoop}" />
                    </ToolTip>
                </ToggleButton.ToolTip>
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton" BasedOn="{StaticResource TB}" >
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource LoopSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Data="{StaticResource LoopSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>

            </ToggleButton>

            <ninactrl:CancellableButton 
                Width="45" Height="45" 
                Command="{Binding MyCommand}" 
                CancelCommand="{Binding MyCancelCommand}" 
                ButtonImage="{Binding MyButtonImage}" 
                CancelButtonImage="{Binding MyCancelButtonImage}"  
                ToolTip="{ns:Loc LblTooltipTakeExposure}"
                Margin="5,0,5,0" HorizontalAlignment="Left"                 
                />            
        </StackPanel>
    </StackPanel>
</UserControl>
