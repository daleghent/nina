<UserControl x:Class="NINA.View.GuiderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NINA.View"
             xmlns:ns="clr-namespace:NINA.Locale"
             
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <UniformGrid Columns="2">


                <GroupBox >
                    <GroupBox.Header>
                        <UniformGrid Columns="2" >
                            <TextBlock Text="{ns:Loc LblGuiderStatus}" VerticalAlignment="Center" FontSize="20"></TextBlock>
                            <Grid Margin="5" Height="40" Width="40" HorizontalAlignment="Right">
                                <Button  Command="{Binding ConnectGuiderCommand}" Visibility="{Binding Guider.Connected, Converter={StaticResource InverseBoolToVisibilityConverter}}" >
                                    <Button.ToolTip>
                                        <ToolTip ToolTipService.ShowOnDisabled="False">
                                            <TextBlock Text="{ns:Loc LblConnect}" />
                                        </ToolTip>
                                    </Button.ToolTip>
                                    <Grid>

                                        <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Data="{StaticResource PowerSVG}" Stretch="Uniform" Margin="5" Visibility="{Binding ConnectGuiderCommand.Execution.IsNotCompleted, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                        </Path>
                                        <Grid Visibility="{Binding ConnectGuiderCommand.Execution, Converter={StaticResource NullToVisibilityConverter}}" >
                                            <local:LoadingAnimation HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ConnectGuiderCommand.Execution.IsNotCompleted, Converter={StaticResource VisibilityConverter}}" Text="..." />
                                        </Grid>
                                    </Grid>
                                </Button>
                                <Button Command="{Binding DisconnectGuiderCommand}" Visibility="{Binding Guider.Connected, Converter={StaticResource VisibilityConverter}, FallbackValue=Hidden}">
                                    <Button.ToolTip>
                                        <ToolTip ToolTipService.ShowOnDisabled="False">
                                            <TextBlock Text="{ns:Loc LblDisconnect}" />
                                        </ToolTip>
                                    </Button.ToolTip>
                                    <Grid>

                                        <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource PowerSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Grid>
                                </Button>
                            </Grid>
                        </UniformGrid>
                    </GroupBox.Header>
                    <Grid>
                        <StackPanel>
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1">
                                <UniformGrid Columns="2" >
                                    <UniformGrid Columns="2" Margin="0 6 0 6" VerticalAlignment="Center">
                                        <TextBlock Text="{ns:Loc LblConnected}" VerticalAlignment="Center"></TextBlock>
                                        <CheckBox IsChecked="{Binding Guider.Connected, FallbackValue=False, Mode=OneWay}" HorizontalAlignment="Left" Height="30" Width="30" BorderBrush="Transparent" Style="{StaticResource CheckmarkCheckbox}" Margin="5" />                                        
                                    </UniformGrid>
                                </UniformGrid>
                            </Border>
                            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1">
                                <UniformGrid Columns="2" >
                                    <UniformGrid Columns="2" Margin="0 6 0 6" VerticalAlignment="Center">
                                        <TextBlock Text="{ns:Loc LblState}"></TextBlock>
                                        <TextBlock Text="{Binding Guider.AppState.State}" Margin="5,0,0,0" />
                                    </UniformGrid>
                                </UniformGrid>
                            </Border>
                        </StackPanel>                        
                    </Grid>
                </GroupBox>
            </UniformGrid>

            <local:GuiderGraph Grid.Row="1" DataContext="{Binding}" IsEnabled="{Binding Guider.Connected }" />
           
        </Grid>
    </Grid>
</UserControl>
