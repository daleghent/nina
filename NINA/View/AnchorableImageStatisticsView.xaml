<UserControl x:Class="NINA.View.AnchorableImageStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NINA.View"
              xmlns:ns="clr-namespace:NINA.Locale"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto" >
        <Grid DataContext="{Binding Statistics}">
            <Grid.RowDefinitions>
                <RowDefinition Height="120" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"  >
                <StackPanel  VerticalAlignment="Top" Margin="5">
                    <StackPanel Orientation="Horizontal" >
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblWidth}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding Width, StringFormat=\{0:#0\}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblHeight}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding Height, StringFormat=\{0:#\}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblMean}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding Mean, StringFormat=\{0:#.00\}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblStDev}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding StDev, StringFormat=\{0:#.00\}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblNrStars}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding DetectedStars, StringFormat=\{0:#\}}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Width="70" Text="{ns:Loc LblHFR}"></TextBlock>
                        <TextBlock HorizontalAlignment="Center" Width="70" VerticalAlignment="Center" Text="{Binding HFR, StringFormat=\{0:#.00\}}" />
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Grid.Row="1" >
                <local:HistogramView MinHeight="80" />
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
