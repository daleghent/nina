<UserControl x:Class="NINA.View.AnchorableSequenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NINA.View"
             xmlns:ns="clr-namespace:NINA.Locale"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">

        <Grid  Height="25" Margin="5" >
            <ProgressBar x:Name="pbSequences" Minimum="0" Maximum="{Binding Sequence.Count}" Value="{Binding ActiveSequenceIndex, Mode=OneWay}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock  Text="{Binding ElementName=pbSequences, Path=Value}" />
                <TextBlock  Text=" / " />
                <TextBlock  Text="{Binding ElementName=pbSequences, Path=Maximum}" />
            </StackPanel>
        </Grid>
        
        
        <GroupBox Header="Active Sequence Details">
            <StackPanel Orientation="Vertical" DataContext="{Binding ActiveSequence}">
                <Grid Height="25" Margin="5" >
                    <ProgressBar x:Name="pbActiveSequence" Minimum="0" Maximum="{Binding TotalExposureCount}" Value="{Binding ProgressExposureCount, Mode=OneWay}" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock  Text="{Binding ElementName=pbActiveSequence, Path=Value}" />
                        <TextBlock  Text=" / " />
                        <TextBlock  Text="{Binding ElementName=pbActiveSequence, Path=Maximum}" />
                    </StackPanel>                    
                </Grid>
                
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                    <UniformGrid Columns="2" VerticalAlignment="Center" >
                        <TextBlock><Run Text="Remaining"/></TextBlock>
                        <TextBlock Text="{Binding ExposureCount}" Margin="5,0,0,0" />
                    </UniformGrid>
                </Border>
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                    <UniformGrid Columns="2" VerticalAlignment="Center" >
                        <TextBlock><Run Text="Exposure Time"/></TextBlock>
                        <TextBlock Text="{Binding ExposureTime}" Margin="5,0,0,0" />
                    </UniformGrid>
                </Border>
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                    <UniformGrid Columns="2" VerticalAlignment="Center" >
                        <TextBlock><Run Text="Image Type"/></TextBlock>
                        <TextBlock Text="{Binding ImageType}" Margin="5,0,0,0" />
                    </UniformGrid>
                </Border>
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                    <UniformGrid Columns="2" VerticalAlignment="Center" >
                        <TextBlock><Run Text="Filter"/></TextBlock>
                        <TextBlock Text="{Binding FilterType}" Margin="5,0,0,0" />
                    </UniformGrid>
                </Border>
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0 0 0 1" MinHeight="30">
                    <UniformGrid Columns="2" VerticalAlignment="Center" >
                        <TextBlock><Run Text="Binning"/></TextBlock>
                        <TextBlock Text="{Binding Binning}" Margin="5,0,0,0" />
                    </UniformGrid>
                </Border>
                                
            </StackPanel>
            
        </GroupBox>
        <local:CancellableButtonView  Height="35" Margin="10 0 10 0"
                    MyCommand="{Binding StartSequenceCommand}" 
                    MyCancelCommand="{Binding CancelSequenceCommand}" 
                    MyButtonImage="{StaticResource PlaySVG}" 
                    MyCancelButtonImage="{StaticResource CancelSVG}" 
                    MyButtonTooltip="{ns:Loc LblTooltipStartSequence}" />
    </StackPanel>
</UserControl>
