<UserControl x:Class="NINA.View.SequenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ns="clr-namespace:NINA.Locale"
             xmlns:local="clr-namespace:NINA.View"
             xmlns:model="clr-namespace:NINA.Model"
             xmlns:util="clr-namespace:NINA.Utility"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <GroupBox Header="{ns:Loc LblSequence}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <DataGrid  x:Name="grd" ItemsSource="{Binding Sequence}" AutoGenerateColumns="False"  CanUserAddRows="False" SelectedIndex="{Binding SelectedSequenceIdx}" BorderThickness="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="*" Header="Remaining #" Binding="{Binding ExposureCount}" IsReadOnly="True">

                    </DataGridTextColumn>
                    <DataGridTextColumn Width="*" Header="Total #" Binding="{Binding TotalExposureCount}">

                    </DataGridTextColumn>
                    <DataGridTextColumn Width="*" Header="Time" Binding="{Binding ExposureTime}"></DataGridTextColumn>
                    <DataGridComboBoxColumn Width="*" Header="Type" ItemsSource="{Binding Source={StaticResource AppVM}, Path=SeqVM.ImageTypes}" SelectedItemBinding="{Binding ImageType, Mode=TwoWay}" />
                    <DataGridComboBoxColumn Width="*" Header="Filter" ItemsSource="{Binding Source={StaticResource AppVM}, Path=CameraVM.FilterWheelVM.FW.Filters}" DisplayMemberPath="Name" SelectedItemBinding="{Binding FilterType}" SelectedValuePath="Name"></DataGridComboBoxColumn>
                    <DataGridComboBoxColumn Width="*" Header="Binning" ItemsSource="{Binding Source={StaticResource AppVM}, Path=CameraVM.Cam.BinningModes}" DisplayMemberPath="Name" SelectedItemBinding="{Binding Binning}" SelectedValuePath="Name"></DataGridComboBoxColumn>
                    <DataGridTemplateColumn Width="*" Header="Dither">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Dither, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Width="*" Header="Dither Every # Frame" Binding="{Binding DitherAmount}"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 5 0 5">
                <Button Width="40"    Command="{Binding AddSequenceCommand}"  >
                    <Button.ToolTip>
                        <ToolTip ToolTipService.ShowOnDisabled="True">
                            <TextBlock Text="{ns:Loc LblTooltipAddSequence}" />
                        </ToolTip>
                    </Button.ToolTip>
                    <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource AddSVG}" Stretch="Uniform" Margin="5">
                    </Path>
                </Button>
                <Button Width="40"   Command="{Binding RemoveSequenceCommand}"  >
                    <Button.ToolTip>
                        <ToolTip ToolTipService.ShowOnDisabled="True">
                            <TextBlock Text="{ns:Loc LblTooltipRemoveSequence}" />
                        </ToolTip>
                    </Button.ToolTip>
                    <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource SubstractSVG}" Stretch="Uniform" Margin="5">
                    </Path>
                </Button>
                <local:CancellableButtonView 
                    MyCommand="{Binding StartSequenceCommand}" 
                    MyCancelCommand="{Binding CancelSequenceCommand}" 
                    MyButtonImage="{StaticResource PlaySVG}" 
                    MyCancelButtonImage="{StaticResource CancelSVG}" 
                    MyButtonTooltip="{ns:Loc LblTooltipStartSequence}" />
            </StackPanel>            
        </Grid>       
    </GroupBox>
</UserControl>
