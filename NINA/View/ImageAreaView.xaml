<UserControl x:Class="NINA.View.ImageAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ns="clr-namespace:NINA.Locale"
             xmlns:local="clr-namespace:NINA.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />                
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="40" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  Margin="2.5" Click="ButtonZoomIn_Click">
                <Button.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="False">
                        <TextBlock Text="{ns:Loc LblZoomIn}" />
                    </ToolTip>
                </Button.ToolTip>
                <Grid>
                    <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource ZoomIn}" Stretch="Uniform" Margin="5">
                    </Path>
                </Grid>
            </Button>
            <Button Grid.Column="1" Margin="2.5" Click="ButtonZoomOut_Click">
                <Button.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="False">
                        <TextBlock Text="{ns:Loc LblZoomOut}" />
                    </ToolTip>
                </Button.ToolTip>
                <Grid>
                    <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource ZoomOut}" Stretch="Uniform" Margin="5">
                    </Path>
                </Grid>
            </Button>
            <Button Grid.Column="2" Margin="2.5" Click="ButtonZoomReset_Click">
                <Button.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="False">
                        <TextBlock Text="{ns:Loc LblZoomStretch}" />
                    </ToolTip>
                </Button.ToolTip>
                <Grid>
                    <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource Stretch}" Stretch="Uniform" Margin="5">
                    </Path>
                </Grid>
            </Button>


            <local:CancellableButtonView  Grid.Column="4" Margin="2.5"
                                          MyCommand="{Binding PlateSolveImageCommand}" 
                                          MyCancelCommand="{Binding CancelPlateSolveImageCommand}" 
                                          MyButtonImage="{StaticResource  PlatesolveSVG}" 
                                          MyCancelButtonImage="{StaticResource CancelSVG}"                                            
                                          MyButtonTooltip="{ns:Loc LblPlateSolveImage}"/>            

            <ToggleButton Grid.Column="5" IsChecked="{Binding ShowCrossHair}"  Margin="2.5">
                <ToggleButton.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="True">
                        <TextBlock Text="{ns:Loc LblTooltipCrossHair}" />
                    </ToolTip>
                </ToggleButton.ToolTip>
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton" BasedOn="{StaticResource TB}" >
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource CrosshairSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Data="{StaticResource CrosshairSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
            
            <ToggleButton Grid.Column="6" IsChecked="{Binding AutoStretch}"  Margin="2.5">
                <ToggleButton.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="True">
                        <TextBlock Text="{ns:Loc LblTooltipAutoStretch}" />
                    </ToolTip>
                </ToggleButton.ToolTip>
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton" BasedOn="{StaticResource TB}" >
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource WandSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Data="{StaticResource WandSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>            

            <ToggleButton Grid.Column="7" IsChecked="{Binding DetectStars}"  Margin="2.5">
                <ToggleButton.ToolTip>
                    <ToolTip ToolTipService.ShowOnDisabled="True">
                        <TextBlock Text="{ns:Loc LblTooltipDetectStars}" />
                    </ToolTip>
                </ToggleButton.ToolTip>
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton" BasedOn="{StaticResource TB}" >
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundBrush}" Data="{StaticResource StarSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Content">
                                    <Setter.Value>
                                        <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Data="{StaticResource StarSVG}" Stretch="Uniform" Margin="5">
                                        </Path>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
        </Grid>
        <ScrollViewer  PanningMode="Both" Grid.Row="1" x:Name="sv" 
                                             
                       >
            
            <ScrollViewer.Style>
                <Style TargetType="{x:Type ScrollViewer}">
                    <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                    <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Rectangle x:Name="Corner" Grid.Column="1" Fill="{StaticResource BackgroundBrush}" Grid.Row="1"/>
                                    <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.Column="0" Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                                    <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                                    <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ScrollViewer.Style>
            <Grid>
                <Path Fill="{StaticResource ButtonForegroundDisabledBrush}" Opacity="0.2" Data="{StaticResource PictureSVG}" Stretch="Uniform" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" Height="100" Width="100">
                </Path>
                <Grid x:Name="grid">
                    <Image Source="{Binding Image}" VerticalAlignment="Center" HorizontalAlignment="Center" 
                           Width="{Binding ActualWidth, ElementName=sv}"
                           Height="{Binding ActualHeight, ElementName=sv}">
                        
                    </Image>

                    <Grid Visibility="{Binding ShowCrossHair, Converter={StaticResource VisibilityConverter}}">
                        <Path Data="
                              M0.5,0 L0.5,0.4 M0.5,0.6 L0.5,1 
                              M0,0.5 L0.4,0.5 M0.6,0.5 L1,0.5
                              
                              M0.35,0.5 A 0.15,0.15 1 1 1 0.35,0.50001
                              M0.4,0.5 A 0.1,0.1 1 1 1 0.4,0.50001
                              M0.45,0.5 A 0.05,0.05 1 1 1 0.45,0.50001
                              M0.48,0.5 A 0.02,0.02 1 1 1 0.48,0.50001
                              M0.49,0.5 A 0.01,0.01 1 1 1 0.49,0.50001
                              M0.495,0.5 A 0.005,0.005 1 1 1 0.495,0.50001
                              " 
                              Stretch="Uniform"  Stroke="{StaticResource ButtonBackgroundSelectedBrush}" StrokeThickness="0.5" />                        
                    </Grid>
                    <Grid.LayoutTransform>
                        <ScaleTransform x:Name="scaleTransform" />
                    </Grid.LayoutTransform>
                </Grid>
                
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
