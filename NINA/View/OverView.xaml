<UserControl x:Class="NINA.View.OverView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NINA.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="5"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="2*"/>
            <RowDefinition Height="5" />
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
            <Grid Margin="0,0,1,1">
                <local:ImageControlView DataContext="{Binding ImagingVM.ImageControl}" />
            </Grid>
      
        <GridSplitter Grid.Column="1"  HorizontalAlignment="Stretch" />
        <GridSplitter Grid.ColumnSpan="3" Grid.Row="1" HorizontalAlignment="Stretch" />
        <local:SequenceView Grid.Column="2" IsEnabled="False" DataContext="{Binding ImagingVM.SeqVM}" Margin="0,0,0,1" />
        <local:CameraCoolingChartsView Grid.Row="2" Grid.ColumnSpan="3" DataContext="{Binding CameraVM}"></local:CameraCoolingChartsView>
        <GridSplitter Grid.ColumnSpan="3" Grid.Row="3" HorizontalAlignment="Stretch" />
        <local:PHD2Graph Grid.Row="4" Grid.ColumnSpan="3" DataContext="{Binding PHD2VM}" />
    </Grid>
</UserControl>
