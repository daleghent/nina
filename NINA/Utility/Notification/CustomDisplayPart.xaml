<core:NotificationDisplayPart x:Class="NINA.Utility.Notification.CustomDisplayPart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NINA.Utility.Notification"
             xmlns:core="clr-namespace:ToastNotifications.Core;assembly=ToastNotifications"
             mc:Ignorable="d"  Background="Gray"
             d:DesignHeight="60" d:DesignWidth="250"
             d:DataContext="{d:DesignInstance local:CustomNotification, IsDesignTimeCreatable=False}" >
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Background="{StaticResource ButtonBackgroundBrush}">
        <TextBlock Margin="5" VerticalAlignment="Center" Text="{Binding Message}" TextWrapping="Wrap" Foreground="{StaticResource ButtonForegroundBrush}" />
    </Border>
</core:NotificationDisplayPart>